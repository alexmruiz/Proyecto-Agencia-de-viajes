server:
  port: 8888 
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alexmruiz/configuracion_servicios.git 
        default-label: main
        #fail-fast: true # Opcional, para manejar fallos rápidamente
